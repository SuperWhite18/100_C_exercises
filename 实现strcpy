char *strcpy(char* str1, const char* str2)
{
	if (*str1 == NULL || *str2 == NULL)
	{
		printf("输入字符串有误\n");
		return NULL;
	}
	int i = 0;
	while (1)
	{
		str1[i] = str2[i];
		if (str1[i++] == '\0')
			break;
	}
	return str1;
}
