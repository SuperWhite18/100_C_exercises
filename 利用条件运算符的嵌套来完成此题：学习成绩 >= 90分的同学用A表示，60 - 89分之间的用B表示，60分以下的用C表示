#include<stdio.h>
int main()
{
	printf("please enter your success :");
	int success;
	scanf_s("%d", &success);
	printf("The success is : %s", success > 90 ? "A" : success > 60 ? "B" : "C");
	return 0;
}
